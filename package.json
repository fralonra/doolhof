{
  "name": "labyrinth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "prepare": "npm run build",
    "lint": "standard --fix src/*.js | snazzy",
    "test": "npm run lint && npm run test:unit && npm run test:bench",
    "test:unit": "tap tests/*.js --cov --coverage-report=lcov"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^1.1.0",
    "rollup-plugin-filesize": "^6.0.0",
    "rollup-plugin-terser": "^4.0.2",
    "snazzy": "^8.0.0",
    "standard": "^12.0.1",
    "tap": "^12.4.0",
    "tape": "^4.9.2"
  },
  "dependencies": {
    "chalk": "^2.4.2"
  }
}
